name: floating_bottom_nav
description: Floating bottom navigation package for flutter.
version: 0.0.1
homepage: https://github.com/gairick-saha/floating_bottom_nav

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
